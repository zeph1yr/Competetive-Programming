#include <iostream>
using namespace std;

int main() {
  //freopen("countin.txt", "r", stdin);
	//freopen("countout.txt", "w", stdout);

    int N;
    cin >> N;
    for(int x=1; x<=N; ++x)
    {
        cout << x <<endl;
    }
	return 0;
}
