#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int a1=0,a2=0;
    string s1,s2;
    cin>>s1>>s2;
    for(char i:s1){a1*=i-'A'+1;}
    for(char i:s2){a2*=i-'A'+1;}
    if(a1%47==a2%47)cout<<"GO";
   else cout<<"STAY";
}
